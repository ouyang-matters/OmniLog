{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the OmniLog desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "store:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
