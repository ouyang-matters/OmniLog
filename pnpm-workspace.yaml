# Only the desktop app is a pnpm workspace package. `packages/shared` is
# consumed directly as TypeScript source (via a Vite alias + tsconfig paths),
# so it is intentionally NOT a linked workspace package - this keeps the repo
# working on cloud-synced drives that don't support symlinks.
packages:
  - "apps/desktop"
