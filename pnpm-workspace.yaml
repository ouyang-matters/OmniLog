# Client apps and shared packages. `packages/shared` and `packages/core` are
# consumed as TypeScript source via Vite aliases + tsconfig paths (no build
# step during development). They are listed here so pnpm installs their
# devDependencies.
packages:
  - "apps/desktop"
  - "packages/core"
  - "packages/ui"
